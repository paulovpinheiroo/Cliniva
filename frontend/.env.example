# Supabase — gerencie as credenciais no painel do Supabase (Project Settings > API)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API backend. Em build de produção aponta para o Fly automaticamente;
# use para sobrescrever (ex.: local apontando pro remoto).
#VITE_API_URL=https://cliniva-backend.fly.dev